/* Bootstrap bundle placeholder
   Replace this file with the official bootstrap.bundle.min.js for full functionality.
   To get the official file, download from https://getbootstrap.com and place the minified bundle here.
*/
(function(){
  // Minimal shim to avoid errors if code expects a global 'bootstrap' object.
  if (typeof window.bootstrap === 'undefined') {
    window.bootstrap = {};
    console.warn('Local bootstrap.bundle.min.js placeholder loaded — replace with official bundle for full Bootstrap JS features.');
  }
})();
